apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stoat.fullname" . }}-test-connection"
  labels:
    {{- include "stoat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stoat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
