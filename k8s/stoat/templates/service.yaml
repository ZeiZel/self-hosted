apiVersion: v1
kind: Service
metadata:
  name: {{ include "stoat.fullname" . }}
  labels:
    {{- include "stoat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stoat.selectorLabels" . | nindent 4 }}
