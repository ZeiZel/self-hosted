apiVersion: v1
kind: Service
metadata:
  name: {{ include "notesnook.fullname" . }}
  labels:
    {{- include "notesnook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "notesnook.selectorLabels" . | nindent 4 }}
