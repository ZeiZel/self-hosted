apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.youtrack.name }}-deployment
  namespace: {{ .Values.youtrack.namespace }}
  labels:
    components: {{ .Values.youtrack.components }}
spec:
  replicas: {{ .Values.youtrack.replicas }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: {{ .Values.youtrack.app }}
      components: {{ .Values.youtrack.components }}
  template:
    metadata:
      labels:
        components: {{ .Values.youtrack.components }}
    spec:
      containers:
        - name: {{ .Values.youtrack.name }}
          image: '{{ .Values.youtrack.image }}:{{ .Values.youtrack.version }}'
          volumeMounts:
            - name: opt-{{ .Values.youtrack.name }}-data
              mountPath: /opt/{{ .Values.youtrack.name }}/data
            - name: opt-{{ .Values.youtrack.name }}-conf
              mountPath: /opt/{{ .Values.youtrack.name }}/conf
            - name: opt-{{ .Values.youtrack.name }}-logs
              mountPath: /opt/{{ .Values.youtrack.name }}/logs
            - name: opt-{{ .Values.youtrack.name }}-backups
              mountPath: /opt/{{ .Values.youtrack.name }}/backups
      volumes:
        - name: opt-{{ .Values.youtrack.name }}-data
          persistentVolumeClaim:
            claimName: {{ .Values.youtrack.name }}-data-pv-claim
        - name: opt-{{ .Values.youtrack.name }}-conf
          persistentVolumeClaim:
            claimName: {{ .Values.youtrack.name }}-conf-pv-claim
        - name: opt-{{ .Values.youtrack.name }}-logs
          persistentVolumeClaim:
            claimName: {{ .Values.youtrack.name }}-logs-pv-claim
        - name: opt-{{ .Values.youtrack.name }}-backups
          persistentVolumeClaim:
            claimName: {{ .Values.youtrack.name }}-backups-pv-claim
