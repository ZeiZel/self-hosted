apiVersion: v1
kind: Service
metadata:
  labels: {{ .Values.name }}-clusterip
spec:
  type: {{ .Values.service.type }}
  selector:
    components: {{ .Values.components }}
  ports:
    - port: {{ .Values.port }}
      protocol: TCP
