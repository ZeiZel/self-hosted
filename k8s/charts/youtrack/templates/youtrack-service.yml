apiVersion: v1
kind: Service
metadata:
  labels: {{ .Values.youtrack.name }}-clusterip
spec:
  type: ClusterIP
  selector: {{ .Values.youtrack.components }}
  ports:
    - port: {{ .Values.youtrack.port }}
      protocol: TCP
