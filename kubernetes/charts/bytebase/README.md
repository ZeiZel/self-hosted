# Bytebase Helm Chart

Helm chart for deploying Bytebase database schema change and version control in Kubernetes cluster.

## Installation

```bash
helm install bytebase ./charts/bytebase -n infrastructure --create-namespace
```

## Configuration

See `values.yaml` for all available configuration options.





