apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bytebase.fullname" . }}-test-connection"
  labels:
    {{- include "bytebase.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox:1.36
      command: ['wget']
      args: ['--spider', '-q', 'http://{{ include "bytebase.fullname" . }}:{{ .Values.service.port }}']
