apiVersion: v1
kind: Service
metadata:
  name: hub
  namespace: {{ .Values.namespace }}
  labels:
    app: hub
spec:
  type: {{ .Values.hub.service.type }}
  ports:
    - port: {{ .Values.hub.service.port }}
      targetPort: 8080
      name: http
  selector:
    app: hub
