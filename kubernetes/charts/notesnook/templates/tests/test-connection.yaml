apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notesnook.fullname" . }}-test-connection"
  labels:
    {{- include "notesnook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "notesnook.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
