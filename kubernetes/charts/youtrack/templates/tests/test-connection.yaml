apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "youtrack.fullname" . }}-test-connection"
  labels:
    {{- include "youtrack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: curl
      image: curlimages/curl:8.5.0
      command: ['curl']
      args: ['-sf', 'http://{{ include "youtrack.fullname" . }}:{{ .Values.service.port }}/api/admin/healthcheck']
