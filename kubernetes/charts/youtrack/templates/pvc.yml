apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.youtrack.name }}-pvc
  namespace: {{ .Values.youtrack.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: storage-class-1
  resources:
    requests:
      storage: 1Gi
