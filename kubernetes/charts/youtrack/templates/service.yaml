apiVersion: v1
kind: Service
metadata:
  name: youtrack
  namespace: {{ .Release.Namespace }}
  labels:
    app: youtrack
spec:
  type: {{ .Values.youtrack.service.type }}
  ports:
    - port: {{ .Values.youtrack.service.port }}
      targetPort: 8080
      name: http
  selector:
    app: youtrack
