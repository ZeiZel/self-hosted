apiVersion: v1
kind: Secret
metadata:
  name: {{ include "minio.fullname" . }}-secret
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  access-key: {{ .Values.minioConfig.accessKey | b64enc }}
  secret-key: {{ .Values.minioConfig.secretKey | b64enc }}
