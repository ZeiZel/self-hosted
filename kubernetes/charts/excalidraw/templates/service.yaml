apiVersion: v1
kind: Service
metadata:
  name: excalidraw
  namespace: {{ .Release.Namespace }}
  labels:
    app: excalidraw
  {{- if .Values.consul.enabled }}
  annotations:
    consul.hashicorp.com/service-name: "excalidraw"
    consul.hashicorp.com/service-port: {{ .Values.excalidraw.service.port | quote }}
    consul.hashicorp.com/service-tags: "excalidraw,drawing,productivity"
  {{- end }}
spec:
  type: {{ .Values.excalidraw.service.type }}
  ports:
    - port: {{ .Values.excalidraw.service.port }}
      targetPort: 80
      name: http
  selector:
    app: excalidraw

{{- if .Values.server.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: excalidraw-server
  namespace: {{ .Release.Namespace }}
  labels:
    app: excalidraw-server
spec:
  type: {{ .Values.server.service.type }}
  ports:
    - port: {{ .Values.server.service.port }}
      targetPort: 3002
      name: http
    - port: {{ .Values.server.config.socketPort }}
      targetPort: {{ .Values.server.config.socketPort }}
      name: socket
      protocol: TCP
  selector:
    app: excalidraw-server
{{- end }}
