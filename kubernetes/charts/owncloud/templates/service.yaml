apiVersion: v1
kind: Service
metadata:
  name: owncloud
  namespace: {{ .Values.namespace }}
  labels:
    app: owncloud
spec:
  type: {{ .Values.owncloud.service.type }}
  ports:
    - port: {{ .Values.owncloud.service.port }}
      targetPort: 8080
      name: http
  selector:
    app: owncloud
