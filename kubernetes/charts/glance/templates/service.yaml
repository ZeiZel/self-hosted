apiVersion: v1
kind: Service
metadata:
  name: {{ include "glance.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "glance.labels" . | nindent 4 }}
  {{- if .Values.consul.enabled }}
  annotations:
    consul.hashicorp.com/service-name: {{ include "glance.fullname" . }}
    consul.hashicorp.com/service-port: "{{ .Values.glance.service.port }}"
    consul.hashicorp.com/service-tags: "{{ join "," .Values.consul.tags }}"
  {{- end }}
spec:
  type: {{ .Values.glance.service.type }}
  ports:
    - port: {{ .Values.glance.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "glance.selectorLabels" . | nindent 4 }}





