# Glance Helm Chart

Helm chart for deploying Glance dashboard in Kubernetes cluster.

## Installation

```bash
helm install glance ./charts/glance -n infrastructure --create-namespace
```

## Configuration

See `values.yaml` for all available configuration options.





