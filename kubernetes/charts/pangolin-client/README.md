# Pangolin Client Helm Chart

Helm chart for deploying Pangolin VPN client in Kubernetes cluster.

## Installation

```bash
helm install pangolin-client ./charts/pangolin-client -n infrastructure --create-namespace
```

## Configuration

See `values.yaml` for all available configuration options.





