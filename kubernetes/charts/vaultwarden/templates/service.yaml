apiVersion: v1
kind: Service
metadata:
  name: vaultwarden
  namespace: {{ .Release.Namespace }}
  labels:
    app: vaultwarden
spec:
  type: {{ .Values.vaultwarden.service.type }}
  ports:
    - port: {{ .Values.vaultwarden.service.port }}
      targetPort: 80
      name: http
  selector:
    app: vaultwarden
