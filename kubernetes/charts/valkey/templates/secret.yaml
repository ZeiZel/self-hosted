{{- if .Values.auth.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: valkey-auth
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  password: {{ .Values.auth.password | quote }}
{{- end }}
