{{- if .Values.auth.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: valkey-auth
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
  password: {{ .Values.auth.password }}
{{- end }}
