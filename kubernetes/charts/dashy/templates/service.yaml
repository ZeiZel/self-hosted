apiVersion: v1
kind: Service
metadata:
  name: dashy
  namespace: {{ .Release.Namespace }}
  labels:
    app: dashy
spec:
  type: {{ .Values.dashy.service.type }}
  ports:
    - port: {{ .Values.dashy.service.port }}
      targetPort: 80
      name: http
  selector:
    app: dashy
