# youtrack release overrides.
# Base config lives in charts/youtrack/values.yaml under the "chart-base:" key.
# Add env-specific overrides here, nested under "chart-base:" (subchart scoping).
# Example:
#   chart-base:
#     workloads:
#       - containers:
#           - name: youtrack
#             env:
#               - name: EXAMPLE
#                 value: "value"
