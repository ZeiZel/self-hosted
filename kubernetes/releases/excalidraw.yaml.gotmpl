# excalidraw release overrides.
# Base config lives in charts/excalidraw/values.yaml under the "chart-base:" key.
# Add env-specific overrides here, nested under "chart-base:" (subchart scoping).
# Example:
#   chart-base:
#     workloads:
#       - containers:
#           - name: excalidraw
#             env:
#               - name: EXAMPLE
#                 value: "value"
