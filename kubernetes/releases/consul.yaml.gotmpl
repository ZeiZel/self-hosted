# consul release overrides.
# Base config lives in charts/consul/values.yaml under the "chart-base:" key.
# Add env-specific overrides here, nested under "chart-base:" (subchart scoping).
# Example:
#   chart-base:
#     workloads:
#       - containers:
#           - name: consul
#             env:
#               - name: EXAMPLE
#                 value: "value"
