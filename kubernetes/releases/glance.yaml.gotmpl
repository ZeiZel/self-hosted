# glance release overrides.
# Base config lives in charts/glance/values.yaml under the "chart-base:" key.
# Add env-specific overrides here, nested under "chart-base:" (subchart scoping).
# Example:
#   chart-base:
#     workloads:
#       - containers:
#           - name: glance
#             env:
#               - name: EXAMPLE
#                 value: "value"
