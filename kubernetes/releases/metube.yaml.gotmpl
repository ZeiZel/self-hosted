# metube release overrides.
# Base config lives in charts/metube/values.yaml under the "chart-base:" key.
# Add env-specific overrides here, nested under "chart-base:" (subchart scoping).
# Example:
#   chart-base:
#     workloads:
#       - containers:
#           - name: metube
#             env:
#               - name: EXAMPLE
#                 value: "value"
