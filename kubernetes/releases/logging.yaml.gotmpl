# logging release overrides.
# Base config lives in charts/logging/values.yaml under the "chart-base:" key.
# Add env-specific overrides here, nested under "chart-base:" (subchart scoping).
# Example:
#   chart-base:
#     workloads:
#       - containers:
#           - name: logging
#             env:
#               - name: EXAMPLE
#                 value: "value"
