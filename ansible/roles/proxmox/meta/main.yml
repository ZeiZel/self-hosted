---
# Meta information for proxmox role

galaxy_info:
  author: zeizel
  description: |
    Unified Proxmox role for VM provisioning and management.
    Combines: proxmox_setup, proxmox_terraform, proxmox_post_provision

    Tags:
      - proxmox: Run all Proxmox tasks
      - proxmox-setup: Setup and verify Proxmox host (API, templates, storage)
      - terraform: Provision VMs with Terraform
      - post-provision: Post-provisioning checks and verification
  company: self-hosted
  license: MIT
  min_ansible_version: "2.9"
  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy
        - noble
    - name: Debian
      versions:
        - bullseye
        - bookworm

dependencies: []
