---
# Service management for VPS

- name: Disable unnecessary services
  ansible.builtin.systemd:
    name: "{{ item }}"
    enabled: false
    state: stopped
  loop: "{{ vps_disabled_services }}"
  failed_when: false
