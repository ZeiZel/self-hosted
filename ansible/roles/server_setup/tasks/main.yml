---
- name: Include packages installation
  include_tasks: packages.yml

- name: Include Pangolin client setup
  include_tasks: pangolin_client.yml
  when: pangolin_client_enabled | default(false)

- name: Include SSH configuration for clients
  include_tasks: ssh_config.yml
  when: pangolin_client_enabled | default(false)

