app:
  dashboard_url: "https://{{ pangolin_domain }}"

domains:
  domain1:
    base_domain: "{{ pangolin_domain }}"
    cert_resolver: "letsencrypt"

server:
  secret: "{{ vault_pangolin_server_secret }}"

gerbil:
  base_endpoint: "{{ pangolin_domain }}"

flags:
  require_email_verification: false
  disable_signup_without_invite: true
  disable_user_create_org: true

# Опционально: настройки SMTP для email уведомлений
# smtp:
#   host: "smtp.gmail.com"
#   port: 587
#   username: "your-email@gmail.com"
#   password: "your-app-password"
#   from: "noreply@{{ pangolin_domain }}"
