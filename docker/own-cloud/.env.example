# OwnCloud Configuration
# OwnCloud version
OWNCLOUD_VERSION=latest

# HTTP port for OwnCloud
HTTP_PORT=8080

# Domain configuration
OWNCLOUD_DOMAIN=localhost
OWNCLOUD_TRUSTED_DOMAINS=localhost

# Admin credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
