services:
  db:
    image: postgres:${PG_VERSION:-18}
    restart: always
    environment:
      - POSTGRES_PASSWORD=teamcity_password
      - POSTGRES_USER=teamcity_user
      - POSTGRES_DB=teamcity_db
    volumes:
      - pgdata:/var/lib/postgresql/${PG_VERSION:-18}/docker
    ports:
      - 5433:5432
  teamcity:
    image: jetbrains/teamcity-server:${TEAMCITY_VERSION:-2026.1.2}
    ports:
      - "8112:8111"
    volumes:
      - ./data_dir:/data/teamcity_server/datadir
      - ./teamcity-server-logs:/opt/teamcity/logs
    depends_on:
      - db
  teamcity-agent-1:
    image: jetbrains/teamcity-agent:${TEAMCITY_VERSION:-2026.1.2}-linux-sudo
    privileged: true
    volumes:
      - ./agents/agent-1/conf:/data/teamcity_agent/conf
    environment:
      - DOCKER_IN_DOCKER=start
  teamcity-agent-2:
    image: jetbrains/teamcity-agent:${TEAMCITY_VERSION:-2026.1.2}-linux-sudo
    privileged: true
    volumes:
      - ./agents/agent-2/conf:/data/teamcity_agent/conf
    environment:
      - DOCKER_IN_DOCKER=start
volumes:
  pgdata:
