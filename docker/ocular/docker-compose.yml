---
services:
  ocular:
    image: ghcr.io/simonwep/ocular:v2.4.0
    restart: unless-stopped
    volumes: [./data:/data/genesis]
    ports: [3030:80]
    environment:
      - GENESIS_JWT_SECRET
      - GENESIS_CREATE_USERS
      - GENESIS_JWT_TOKEN_EXPIRATION
